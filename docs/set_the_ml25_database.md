# Set up the MovieLens database

1. Download the dataset from the following link: https://grouplens.org/datasets/movielens/.
2. Place the directory `ml-25` within the `/dataset` directory.
3. Run the script `setup_ml_25_db.py` under `/src/commandline_scripts/db_setup_scripts/`.