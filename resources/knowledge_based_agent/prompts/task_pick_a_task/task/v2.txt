The latest input from the user is:
{user_input}

The current objective is:
{current_objective}

You need to pick a task from the following list.

{all_possible_tasks}

If there is no objective, then the priority is to pick a task that helps set an objective.

Otherwise, the priority is to pick a task that can achieve this objective given
the current information.

Currently, the names of the information available is listed below.
Each item represents a collection of information that can be used for other tasks.
Names containing "result" typically can be used to generate a response.
{information_cache_keys}

Utilize the existing information above in determining which task is most relevant.
ONLY IF THE INFORMATION LISTED ABOVE IS NOT SUFFICIENT CAN DRAW_MORE_INFO BE SELECTED!!!!

The following tasks have already been executed.
IF AN ALREADY EXECUTED TASK IS PICKED AGAIN, ADD TO REASONING WHY IT HAS TO BE REPICKED.
{executed_tasks}

1. "task_name" is a single string as the name of the task.
2. "reasoning" is a single string as an explanation to why this task is picked.

Example outputs:
{{"task_name": "Task_A", "reasoning": "Given the information xxxx, Task_A makes most sense because... On the other hand, the other task TASK_B, ... TASK_C ... TASK_D ..."}}
{{"task_name": "Task_B", "reasoning": "Task_B makes sense because ... On the other hand, the other tasks ..."}}
