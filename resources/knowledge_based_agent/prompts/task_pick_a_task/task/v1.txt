The latest input from the user is: {user_input}.

The current objective is {current_objective}.

The following tasks have already been executed. Make sure that you do not pick repetitive tasks
unless such repetitive tasks are necessary.
{executed_tasks}

You need to pick a task from the following list.

{all_possible_tasks}

If there is no objective, then the priority is to pick a task that helps set an objective.

Otherwise, the priority is to pick a task that can achieve this objective given
the current information.

If all available data relevant to the objective are present given the names of the information
listed above, then the priority should be to formulate a response using relevant information.

Otherwise, other tasks might be needed to collect more information from the user or from other
agents, or to execute certain actions.

Currently, the names of the information available is listed below.
{information_cache_keys}

task_name is a single string as the name of the task.
reasoning is a single string as an explanation to why this task is picked.

Example outputs:
{{"task_name": "Task_A", "reasoning": "Given the information xxxx, Task_A makes most sense because... On the other hand, the other task TASK_B, ... TASK_C ... TASK_D ..."}}
{{"task_name": "Task_B", "reasoning": "Task_B makes sense because ... On the other hand, the other tasks ..."}}
